import 'package:flutter/material.dart';
import 'package:raai/feature/add_reads/presentation/view/widget/add_reads_screen_body.dart';

class AddReadsScreen extends StatelessWidget {
  const AddReadsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: AddReadsScreenBody());
  }
}
